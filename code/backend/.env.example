# PostgreSQL connection string injected by runtime or docker compose.
DATABASE_URL=

# Preferred HTTP port for the backend server.
PORT=8080

# Fallback HTTP port used when PORT is unset.
APP_PORT=8080
